<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:name="com.linecorp.planetkit.quickstart.groupVideoCall.GroupVideoCallApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:targetApi="35">
        <activity
            android:name=".GroupVideoMainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".GroupVideoCallActivity"
            android:launchMode="singleTop"/>

        <service
            android:name="com.linecorp.planetkit.quickstart.groupVideoCall.GroupVideoNotificationService"
            android:foregroundServiceType="mediaPlayback|microphone" />
    </application>
</manifest>